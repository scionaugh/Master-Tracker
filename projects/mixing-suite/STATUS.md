# Mixing Suite

**Last Updated:** 2026-06-18
**Status:** ACTIVE — next-beta (v0.2.0/β3) unified build COMPLETE on branch `next-beta` (all 11 runner phases + audit, SHIP-READY); UI cohesion + readability pass IMPLEMENTED (2026-06-18); UI Changes Round 2 (§8–§16) IMPLEMENTED + audit PASS (2026-06-18); **UI Verify & Complete Round 3 DONE (2026-06-18) — §0–§16 audited for render-path effectiveness: all DONE, 2 real gaps FIXED (§9 engraved value readouts via shared drawLabel inset; §7.3 ScioSee now ships in Block mode). Root cause of "doesn't render" = round-2 work was uncommitted/uninstalled, so the loaded DAW build lagged the working tree; all 9 plugins clean-rebuilt + reinstalled (auval lists all, 18/18 Info.plist)**; **DSP features §17 (Dynamic-EQ Ratio/Range/Auto) + §18 (ScioSee Totalyser ms ballistics) DONE 2026-06-18, independent audit PASS** — the two former DSP-pass follow-ups are now closed; pending: pre-ship manual items only

## Current Focus
Suite spans eight plugins (ScioSee added as the eighth). The four original plugins (ScioTube, ScioTape, ScioGlue, ScioSpace) are at beta v2 with stochastic noise complete. Both EQs (ScionaughEQ, ScionaughEQP) have prototype + UI v2 complete (2026-06-12). ScionaughMB (multiband) now has DSP (P0-P8) and UI v1 (U0-U5) committed to git (cc69c22); a v2 upgrade spec (4-way dynamics, 3-slot sat stack, threshold-on-bar) is written but uncommitted, with code still lagging the notes. NEW (2026-06-17): ScioSee (code target ScionaughAnalyser), a unified spectrum/scope/stereo analyzer with a pinned loudness meter, is fully spec'd with phased code prompts written to `Plugins/ScionaughAnalyser/`; it introduces a shared analysis+visualization layer in `Shared/` so display upgrades propagate suite-wide. Design done, no code yet.

**DONE 2026-06-17 — next-beta (0.2.0/β3) autonomous build:** all 11 runner phases + final audit complete and committed on branch `next-beta`, all 7 plugins build green. Delivered: shared global settings store + gear panel; consent unification (no more hard-coded setConsentGranted(true) — reads dataCollectionConsent); in-plugin feedback modal; signal/stability probe (in/out meters, NaN/Inf, CPU, clean-exit, in+out spectral scalars); playback/export oversampling via isNonRealtime (removed audio-thread initProcessing); per-instance undo/redo; metal knob + faceplate (cached body) + per-plugin whole-metal colour ramps (all 7 heroes match the locked table); ScioEQ HPF/LPF slope cascade + resonance sensitivity; ScioEQP vertical-drag fix; opt-in staged auto-update + state-version; behavioural + preset events. New shared header-only modules: ScionaughSettings.h, ScionaughFeedback.h, ScionaughSignalProbe.h, ScionaughUndo.h, ScionaughBehavior.h. Audit verdict SHIP-READY (report: `docs/prompts/next_beta_audit_report.md`); tests 16/17 (the 1 fail is a pre-existing stale codename-slug test, not a regression). Suite-level next focus: parameter tuning pass and the next-beta pre-ship items below.

## Recent Fixes
- **2026-06-18 · DSP features §17 + §18 DONE — independent audit PASS** (prompt `docs/prompts/dsp_features_build_prompt.md`; verdict `docs/prompts/dsp_features_audit_report.md`). Two audio-path features off `docs/ui_cohesion_readability_spec.md`. **§17 Dynamic-EQ Ratio/Range/Auto (ScioEQ):** three per-band APVTS params added — `dynRatio` (log 1..20, default 2.0, top-of-range = true limiting), `dynRange` (0..24 dB, default 6.0, ±ceiling on applied gain), `dynAuto` (bool, default false). DSP in `SVFBand.h`: offset = over/under-threshold × (1 − 1/ratio) clamped to ±range; Auto adds a ~300 ms program follower → tracking threshold (progDb − 3 dB) + makeup that restores average loudness; attack/release + **Off path bit-identical** (dynamics block only runs when dynMode≠Off). `STATE_VERSION` bumped 1→2; legacy state lacking the new nodes loads them at defaults (APVTS replaceState). §8 node-anchored drawer grew a second dynamics row (RATIO/RANGE knobs + AUTO toggle), `kPanelHDyn` 284→372. **§18 ScioSee Totalyser ms ballistics:** `SpectrumConfig` gained `useMsBallistics`(default OFF → EQ/EQP/MB unchanged) + rise 2 ms / release 0.3 s / peak-hold 0.1 s / infinite-hold (the §7.3 DIGICheck defaults); per-frame coeff = exp(−1/(timeSec·framesPerSec)) with framesPerSec = 4·fs/fftSize (75%-overlap hop rate). Block bars + per-bin curve rise fast → hold → release; peak markers hold then fall or stick under Infinite-Hold; `resetPeakHold()` clears held image. ScioSee gear: Speed→rise/release presets (Medium=2 ms/0.3 s), new Hold box (50/100/500 ms/∞), Reset-Pk button. Realtime-safe (analyser is UI-thread; no audio-thread alloc). Tests extended + green: `ScionaughEQTests` (new ratio/range cases), `ScionaughEQPresetTests` (TPd new-ids/defaults + legacy back-compat), `ScioSpectrumParityTests` (steady/release/inf-hold/reset). Full `scripts/rebuild_install.sh` exit 0, all 9 plugins VST3+AU installed, `auval` lists all (ScioSound + legacy), 18/18 Info.plist. Independent Opus-4.8 subagent audit = PASS, no correctness issues. **Note:** the ScioEQ `PluginEditor` working tree also carries the prior uncommitted ScioSee-retrofit/UI changes — §17/§18 are entangled with those at commit time (stage selectively). Branch `next-beta`, uncommitted.
- **2026-06-18 · UI Verify & Complete Round 3 DONE — audit report `docs/prompts/ui_verify_complete_audit_report.md`.** Verify-and-fix pass on `docs/ui_cohesion_readability_spec.md` §0–§16 under the rule "code present ≠ done — must be effective in the render path." Dispatched 5 parallel render-path audits + verified the highest-contradiction items by hand (EQ band strip, ScioSee block default, value boxes). **Headline:** round-2's work was genuinely in the render path but lived **only in the uncommitted working tree** (shared LAF +252 uncommitted lines; ScioSee/ScioClip untracked) — no installed bundle carried it, so the white boxes / amber goniometer / line spectrum / small EQP knobs / faint icons the human saw were a **stale loaded build**, not stale code. Operative fix = rebuild+reinstall all 9. **Two real code gaps FIXED:** (§9) value readouts only had `textBoxOutlineColourId=transparentBlack` (minimal path) → added the spec's preferred engraved inset via a new shared `drawLabel` override (recessed slot + faint `theme.frame` border + `metal.hero` top highlight + toned text; detects slider value boxes by parent, inherited by every plugin); (§7.3) ScioSee block mode was wired+reachable but **defaulted to Smooth** (the "line not blocks" symptom) → now ships in Block (`blockToggle.setToggleState(true)` + `setMode(Block)` before `applySettings`). All other §0–§16 items verified DONE with quoted render-path lines (EQ node-anchored cluster + retained selection tab strip per §8; Space goniometer blue via `th.accent`; EQP knobs 64/56px; burnt-orange Glue; cool tone-downs; faceplate 0.55/0.30; header icons 24/min22 → `theme.textPrimary`). Build: `scripts/rebuild_install.sh` all 9 succeeded+installed (VST3+AU, exit 0); `auval -a | grep scio` lists all 9 under ScioSound + legacy names; 18/18 `.component` carry Info.plist. `git diff --stat`: 39 files, +1678/−1031. Branch `next-beta`, uncommitted. Human: rescan plugins in DAW to pick up the rebuilt bundles.
- **2026-06-18 · ACTIVE WORK ITEMS — two build prompts queued.** Spec is `docs/ui_cohesion_readability_spec.md` (§0–§18). (1) **DSP features** — `docs/prompts/dsp_features_build_prompt.md`: §17 dynamic-EQ per-band Ratio/Range/Auto (ScioEQ, new APVTS params + STATE_VERSION bump + back-compat + cluster drawer) and §18 ScioSee Totalyser ms ballistics (rise 2ms/release 0.3s/hold 0.1s/inf-hold). **✅ DONE 2026-06-18 (audit PASS — see top entry).** (2) **UI verify & complete (round 3)** — `docs/prompts/ui_verify_complete_build_prompt.md` (supersedes the round-2 UI prompt): verify-and-fix pass because round 2 reported §0–§16 done but several items don't render (white value boxes, EQ tab strip not removed, ScioSee line not blocks, amber Space goniometer, small EQP knobs, faint header icons) — rule: 'code present' ≠ done, must be in the render path. **TO RUN AFTER DSP.** Build/verify via the fixed `scripts/rebuild_install.sh` (--clean-first _All, keeps legacy Scionaugh bundles, auval + Info.plist checks).
- **2026-06-18 · UI Changes Round 2 IMPLEMENTED — audit PASS** (spec `docs/ui_cohesion_readability_spec.md` incl. the newer §8–§16; wrapper `docs/prompts/ui_changes_round2_build_prompt.md`; verdict `docs/prompts/ui_changes_round2_audit_report.md`). Autonomous 9-phase build on `next-beta`, all 9 plugins build green via `scripts/rebuild_install.sh` (full clean, exit 0), every VST3+AU installed, all 18 AU `.component`s carry Info.plist, `auval -a | grep scio` lists all 9 under both ScioSound + legacy names. Delivered: **P1** shared theming completeness — `ThemePalette.faceplateCornerBrightness` (read by `drawFaceplate`) + theme-colour params on `drawPipeConnector`/`drawDisplayInset`/`drawSectionDivider`, ScioSpace pipe+insets routed to blue (§15); **P2** ScioGlue recoloured oxblood→**burnt orange** (§10 metal+theme hex exactly), oxblood panel borders + tube-glass rim migrated, bottom toolbar evened + "TRANSFORMER" untruncated (§11); **P3** ScioEQ/ScioMB cool tone-down (accent/glow/band/top-stops to ScioSpace calm level) + EQ wordmark/axis lifted (§3.4); **P4** Tube faceplate corner 0.55 / Tape 0.30 (§16); **P5** EQP Pultec dials enlarged (§13); **P6** shared value readouts engraved (text→metal.hero, hard outline dropped, §9); **P7** ScioEQ band editing → **node-anchored overlay cluster** (anchors to node, flips above/below, clamps, stem; right-click Type menu; dashed dynamic-node rings; full-height graph reclaimed, §8); **P8** ScioSee block spectrum → **discrete stacked DIGICheck cells** + greyscale-default/Green/Red/Blue selector + 0.5/1.0/1.5 dB resolution, loudness readout enlarged to scalable labelled rows, **SignalBus publish + local Sidechain overlay** (§7.3); **P9** ScioClip verified already verdigris-complete (no change). Grep-confirmed zero residual Brass*/Amber* in ScioSpace/ScioGlue/ScioEQ. UI-only; two honest deferrals belong to a future DSP pass — §8 EQ Ratio/Range/Auto (params don't exist) and §7.3 ScioSee ms-accurate Totalyser ballistics (analyser uses unitless coeffs). Branch `next-beta`, uncommitted.
- **2026-06-17 · Cohesion round 2 (structural brass leaks).** After rebuild review: faceplate body still brass + rivets/panels brass. Fixes: shared `drawFaceplate` now tints its whole body with the plugin's own `metal.dark` (was global brass BackgroundMid/Deep) so Tube/Tape/Glue/Space backgrounds match identity; `drawRivet`/`drawPanelBorder` got optional colour params (default brass) — ScioGlue's 5 panels now dark red, ScioSpace pad rivets now blue; ScioSpace knob ramp toned down (was too bright); ScioTube `warm`/dark nudged greener (was reading red); ScioEQP gold top-stops desaturated+darkened (were pale cream). ScioMB needs no change — already uses shared drawMetalKnobBody (the ring look was the stale binary). Branch next-beta, uncommitted; rebuild via fixed scripts/rebuild_install.sh (_All targets).
- **2026-06-18 · UI cohesion + readability pass IMPLEMENTED (spec `docs/ui_cohesion_readability_spec.md` done; audit `docs/prompts/ui_cohesion_audit_report.md` verdict PASS).** Autonomous 6-phase build on branch `next-beta`, all 7 plugins build green (VST3+AU), Spatialiser ConvolutionTests pass, UI-only (no DSP touched bar two µm/µs label-string encodings). Phase 0: structural **ThemePalette** added to shared `ScionaughLookAndFeel` (text tiers + accent/frame/grid, defaults to brass → warm plugins byte-identical; `drawButtonText` icon now reads `theme.textPrimary`). Phase 1: Tape `µm`/Compressor `µs`/MB em-dash strings → `juce::String::fromUTF8`, removed overlapping ScioEQP `FREQ` sublabels. Phase 2: header glyph icons standardised to shared `kHeaderIconSize=24` (clamped, all 7 editors); warm-plugin labels lifted to new `TextSecondaryLift`; ScioGlue selector truncation fixed via `drawFittedText` + a shared `positionComboBoxText` override; ScioEQ axis/wordmark contrast lifted (`textMid`). Phase 3: **ScioGlue fully red, ScioSpace fully blue** — every `Brass*`/`Amber*` ref in those two editors replaced with the wired theme (grep-confirmed zero residual); Glue knobs stay red (decision B1); EQ/MB themed to their blue/purple identity; Tube/Tape/EQP keep brass default. Phase 4: Tube/Tape top metal stops desaturated ~20%, ScioGlue oxblood mids saturation-boosted. Phase 5: ScioSpace XY-pad gained a live Wideness×Depth halo + elevation shading (+layout rebalance), ScioEQ spectra receded and band nodes given dark-backing + bright ring for layer separation. **Branch `next-beta`, uncommitted (layered on the prior UI pass + in-progress ScioSee working tree).**
- **2026-06-17 · UI cohesion + readability SPEC written** (`docs/ui_cohesion_readability_spec.md`, no code yet). After rebuild review of all 6 visible plugins. Root cause: per-plugin identity only drives knobs+faceplate; text/labels/frames/displays/tabs still use global brass `ScionaughColours`, so blue (Space) and red (Glue) plugins paint brass chrome = split palettes. Spec covers: structural ThemePalette fix (recommended) vs tactical patches; quick bugs (Tape `µm`→`Âµm` UTF-8 decode, EQP `FREQ` label overlap lines 886-888); readability (warm-plugin label contrast, Glue truncated labels, header-icon size inconsistency Tube osH~20/EQP btnSz18/etc → standardise 24px); knob saturation (Tube/Tape too neon, Glue too washed); display upgrades (Space spatial-pad feedback, EQ layer separation). 2 open decisions: A cohesion approach, B Glue unify toward red(rec)/gold.
- **2026-06-17 · Header icons fixed suite-wide (font glyph → vector).** Gear (settings) and speech-bubble (feedback) buttons on all 7 plugins, plus undo/redo in the shared undo bar, set their icon as a Unicode glyph rendered in the bundled Special Elite typewriter font — which lacks U+2699/U+1F5E8/U+21B6/U+21B7, so they rendered as tofu/garbled and dim. Added `drawButtonText` override in shared `ScionaughLookAndFeel`: intercepts the known glyph codepoints (gear, feedback, undo, redo, close ×) and draws font-independent vector paths in legible brass (`TextPrimary`) with a brighter hover; all other button text falls through to base. Icon inset 0.18 of button height (≈64% fill, sized up from 0.28 per feedback). One shared file, zero per-plugin edits. Branch `next-beta`, uncommitted; needs rebuild.
- **2026-06-17 · ScioEQP frequency dial got the metal body.** The stepped HIGH-BOOST frequency selector (`HFBoostDialSelector::paint`) hand-drew a flat `knobTrack` disc and missed the metal-knob refresh, so it read as a dark ring next to the gold metal knobs. Now calls shared `drawMetalKnobBody` (body box sized so the -9px trim yields a ~18px disc) with the arc pulled inside the lip to match `ScioEQPLookAndFeel::drawRotarySlider`; rim ticks dropped (arc+pointer+Hz label already show the step). Branch `next-beta`, uncommitted; needs rebuild — small dial, eyeball its size after building.
- **2026-06-17 · Faceplate gradient calmed (shared, 4 plugins).** `drawFaceplate` top-right glow was too dramatic on the bright-brass plugins (ScioTube/ScioTape). Hot corner dimmed `metal.warm.withMultipliedBrightness(0.42)` (was 0.65) and the crushed-black far corner lifted `BackgroundDeep.interpolatedWith(BackgroundMid, 0.45)` (was BackgroundDeep), midpoint unchanged. Scales with each plugin's warm alloy so Glue (oxblood)/Space stay subtle. Affects Tube, Tape, Glue, Space (EQ/EQP/MB paint their own flat bg). Branch `next-beta`, uncommitted; needs rebuild.
- **2026-06-17 · Shared metal-knob refinement (all 7 plugins).** Tuned the single shared `renderKnobBody`/`drawRotarySlider` in `Shared/ScionaughLookAndFeel.cpp` after the metal-knob pass read too heavy/clunky. New values: skirt `innerR = outerR*0.90` (was 0.74, thinner knurled ring), knurl contrast `lit*lit*0.11` (was 0.95), knurl alpha 0.65 (was 0.90), drop shadow alpha 0.13 (was 0.45), body size trim `outerR = half-9` (was half-2), lip highlight alpha 0.50 (was 0.85), and a new face-shading control `kTopGrad=0.60` blending the flat-top stops toward hero (was full highlight→dark). Tick geometry kept in sync. Dialled in via an interactive preview. Per-plugin colour ramps untouched. **Branch `next-beta`, working tree (uncommitted); needs macOS rebuild to view.**
- **2026-06-17 · ScioSpace pan insertion-loss bug FIXED** (see mixing-suite-sciospace STATUS): centre pan was −4.5 dB on every insert; replaced constant-power law with a stereo balance law (centre = unity). Code done, needs rebuild.

## Next Actions
- NEXT-BETA PRE-SHIP (branch `next-beta`, build done; all 7 VST3 installed Release + visually checked in Ableton 2026-06-17): RESOLVED in-session — ScioGlue red retuned to dark desaturated oxblood; metal KNOB form now on all 7 (compact metal variant added to EQ/EQP/MB keeping their value arc, be15c62); MB/Tape/Tube reconfigured to Release. REMAINING: (1) create the Supabase `feedback` table + 90-day retention (sendEvent posts to a /feedback endpoint that must exist) and rotate the anon key; (2) code-sign + notarize so the opt-in staged auto-update can actually install; (3) auval AU validation; (4) optional: emit the 2 remaining preset events (preset_tweaked_after_load, preset_abandoned). Then merge `next-beta` → develop and cut the β3 installer.
- ScioSee (ScionaughAnalyser): run the 7-phase extract-first build from `Plugins/ScionaughAnalyser/ScioSee_CodePrompts.md`, starting Phase 1 (SpectrumAnalyzer extraction + retrofit EQ/MB)
- ScioMB: finish U6 + run U-phase tests, then build v2 from `ScioMB_v2_CodePrompts.md`
- Parameter tuning pass across all plugins: identify unusable zones, consider 15%-extreme-zone parametric mapping
- Telemetry redesign: session-based snapshots, delta check, retention/purge (fix foundation before event logging)
- ScioEQ/ScioEQP: parameter tuning + integration testing; resolve EQP 12AU7 / LF-freq reconciliations
- Test all plugins and log issues

## Blockers
<!-- Leave blank if none -->













































## Recent Commits
- 2026-06-18 · 0fe0313 · feat(suite): next-beta UI cohesion (rounds 1–3) + Dynamic-EQ/Totalyser DSP + ScioSee/ScioClip
- 2026-06-17 · be15c62 · fix(ui): dark-desaturated ScioGlue red + compact metal knob for EQ/EQP/MB
- 2026-06-17 · f62dbfb · chore(suite): next-beta final audit report
- 2026-06-17 · 495f755 · feat(update): opt-in staged auto-update + behavioral events + preset analytics
- 2026-06-17 · 2e2ab29 · feat(eq): HPF/LPF slope selector + resonance sensitivity; fix(eqp): vertical drag
