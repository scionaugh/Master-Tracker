# Master Tracker — Root Todo Inbox

<!-- Cross-cutting / infrastructure items that don't belong to a single project. -->
<!-- Format: - [ ] YYYY-MM-DD — item text -->

## Open
- [ ] 2026-06-15 — [SECURITY] Master-Tracker git remote has a GitHub personal access token embedded in the remote URL in plaintext (visible in .git/config). Rotate/revoke that token on GitHub, then switch the remote to SSH (or a credential helper) so no token is stored in config. Repo: github.com/scionaugh/Master-Tracker.

## Done
